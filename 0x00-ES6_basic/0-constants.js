export function taskFirst() {
	// \n
	const task = 'I make use of const when i cam.';
	return task;
}// \n

export function getlist() {
	 return ' is okay';
 }

export function taskNext() {
	let conbination = 'But sometimes let';
	combination += getLast();

	return combination;
}
